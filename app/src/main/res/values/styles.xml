<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:buttonStyle">@style/button</item>
    </style>

    <style name="button" parent="@android:style/Widget.Button">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:textColor">#FFFFFFFF</item>
        <item name="android:shadowRadius">0.2</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@drawable/button</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:minWidth">100dp</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
    </style>

    <!-- Define your custom dialog theme here extending from base -->
    <style name="TimePickerDialogThemeParent" parent="Theme.AppCompat.Light.Dialog">
        <!-- Define color properties as desired -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorCircleProgressBar1</item>
        <item name="colorControlNormal">@color/colorCircleProgressBar1</item>
        <!-- Define window properties as desired -->
        <item name="android:windowNoTitle">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowBackground">@color/colorEnd</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <style name="TimePickerDialogTheme1" parent="TimePickerDialogThemeParent">
        <item name="colorAccent">@color/colorCircleProgressBar1</item>
        <item name="colorControlNormal">@color/colorCircleProgressBar1</item>
    </style>

    <style name="TimePickerDialogTheme2" parent="TimePickerDialogThemeParent">
        <item name="colorAccent">@color/colorCircleProgressBar2</item>
        <item name="colorControlNormal">@color/colorCircleProgressBar2</item>
    </style>

    <style name="TimePickerDialogTheme3" parent="TimePickerDialogThemeParent">
        <item name="colorAccent">@color/colorCircleProgressBar3</item>
        <item name="colorControlNormal">@color/colorCircleProgressBar3</item>
    </style>

    <style name="TimePickerDialogTheme4" parent="TimePickerDialogThemeParent">
        <item name="colorAccent">@color/colorCircleProgressBar4</item>
        <item name="colorControlNormal">@color/colorCircleProgressBar4</item>
    </style>

    <style name="TimePickerDialogTheme5" parent="TimePickerDialogThemeParent">
        <item name="colorAccent">@color/colorCircleProgressBar5</item>
        <item name="colorControlNormal">@color/colorCircleProgressBar5</item>
    </style>

    <style name="TimePickerDialogTheme6" parent="TimePickerDialogThemeParent">
        <item name="colorAccent">@color/colorCircleProgressBar6</item>
        <item name="colorControlNormal">@color/colorCircleProgressBar6</item>
    </style>

</resources>
